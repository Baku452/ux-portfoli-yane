---
import { Icon } from "astro-icon/components";
import type { Labels } from "../content/homepage";

interface Props {
  role: string;
  about: string;
  location: string;
  resumeUrl: string;
  labels: Labels;
}

const { role, about, location, resumeUrl, labels } = Astro.props;
---

<section class="details-grid" aria-label={labels.profileDetails}>
  <article class="role-card">
    <h2>{role}</h2>
    <p>{about}</p>
  </article>
  <div class="quick-links">
    <span>
      <Icon name="hugeicons:location-01" size={20} /> {location}
    </span>
    <a href={resumeUrl} download>
      <Icon name="hugeicons:download-04" size={20} /> {labels.downloadCv}
    </a>
  </div>
</section>
